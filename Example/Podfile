use_frameworks!

target 'dSRelay_Example' do
  pod 'dSRelay', :path => '../'
  pod 'SwiftSocket'
  pod 'thenPromise'

  target 'dSRelay_Tests' do
    inherit! :search_paths

    pod 'dSRelay', :path => '../'
    pod 'SwiftSocket'
    pod 'thenPromise'
    pod 'Quick'
    pod 'Nimble'
    pod 'InstantMock'
  end
end
